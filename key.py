import tkinter as tk
from tkinter import ttk

# Temporarily deprecated
def apikey(api_key_tab):
    # Create a scrollable frame for news entries
    api_key_info = ttk.Label(api_key_tab, text="Development temporarily paused")
    api_key_info.pack(side="top", padx=10, pady=10)